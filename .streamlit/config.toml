[server]
# Streamlit Community Cloud 会忽略 port，但本地开发保留
# headless = true 避免本地自动打开浏览器（CI 友好）
headless = true
enableCORS = false
enableXsrfProtection = true

[browser]
gatherUsageStats = false

[theme]
base = "dark"
